<div class="row mt-5">
    <div class="col-md-6 m-auto">
      <div class="card card-body">
        <h1 class="text-center mb-3"><i class="fas fa-user"></i>  Welcome <%= name %></h1>
        <%- include ('./partials/messages') %>

        <!-- Insert under File add -->
        <!-- Add method="POST" next to form action -->
        <form action="/dashboard">
          <div class="form-group">
            <label for="file">File</label>
            <input
              type="email"
              id="email"
              name="email"
              class="form-control"
              placeholder="Enter Email"
            />
          </div>
          <!-- Inseert above file add -->

          <button type="submit" class="btn btn-primary btn-block">Add File</button>
          <a href="/users/logout" class="btn btn-secondary btn-block">Logout</a>
        </form>
      </div>
    </div>
  </div>

<h1 class="mt-4">Dashbaord</h1>
<p class="lead mb-3">Welcome <%= name %></p>
<a href="/users/logout" class="btn btn-secondary">Logout</a>